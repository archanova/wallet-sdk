export enum Actions {
  Init = 'init',
  Develop = 'develop',
  Deploy = 'deploy',
}

export enum Screens {
  SetupAccount = 'SetupAccount',
  SetupApp = 'SetupApp',
  DevelopApp = 'DevelopApp',
  DeployApp = 'DeployApp',
}
